# Remote Atlas Frontend

Next.js UI for Remote Atlas. Talks only to the backend public API — no AI/API secrets here.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Open http://localhost:3000
